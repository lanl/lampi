To compile LAMPI with CFS support:

CFS support is not included with LAMPI, since it is HP proprietary code.
To compile LAMPI with CFS support, check out the HP module seperatly and
copy or link the code into this directory:

cd ~/lampi/romio-1.2.4/adio/ad_cfs
ln -s  ~/hp/ad_cfs/* .


